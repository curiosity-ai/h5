<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <UsingTask TaskName="GenerateScript" AssemblyFile="$(MSBuildThisFileDirectory)HighFive.Build.dll" />

  <PropertyGroup>
    <NoStdLib>True</NoStdLib>
  </PropertyGroup>

  <ItemGroup>
    <Reference Include="HighFive.dll">
      <HintPath>$(MSBuildThisFileDirectory)HighFive.dll</HintPath>
    </Reference>
  </ItemGroup>

  <Target Name="AfterBuild">
    <GenerateScript
      OutputPath="$(OutputPath)"
	    Configuration="$(Configuration)"
      Assembly="@(IntermediateAssembly)"
	    AssembliesPath="$(MSBuildThisFileDirectory)"
      ProjectPath="$(MSBuildProjectFullPath)">
    </GenerateScript>
  </Target>
</Project>
